# AGENTS.md — 작업 전 반드시 읽어야 하는 지침

이 파일은 이 프로젝트에서 AI 에이전트가 항상 준수해야 하는 작업 규칙입니다.
**모든 작업(대화 시작, 수정 요청, 커밋/푸시) 전에 먼저 이 파일을 읽고 지침에 따라 행동합니다.**

---

## 1. 작업 전 필수 체크리스트

코드/설정 변경이 필요한 작업을 시작하기 전에 아래를 확인한다.

- [ ] **대화 및 작업 시작 전 최신 상태 동기화 (git pull)**: 작업이나 대화를 시작하기 전에 반드시 `git fetch` 및 `git pull --rebase origin main`을 수행하여 원격 최신 상태를 로컬에 반영한다.
- [ ] **API 호출 테스트 금지 (할당량/비용 보호)**: 로컬 검증 시 Gemini API, Pexels/Pixabay API, YouTube API 등 실제 외부 API를 호출하는 테스트 실행은 절대 금지한다. 문법 검사(`py_compile`) 및 오프라인 단위 테스트/Mock 검증만 수행한다.
- [ ] 작업 범위가 모호하면 **임의로 판단하지 않고 질문으로 확인**한다.
- [ ] 소스 수정은 `INSTRUCTIONS.md`에 따라 **사용자 승인 후에만** 진행한다.
- [ ] 변경하려는 기능이 **다른 필드/로직에 어떤 영향을 주는지 영향 범위를 먼저 분석**한다.
  - 예: `full_script`, `hook`, `body`, `cta`, `comment_cta`, `thumbnail_hook` 은 서로 연결되어 있어 하나만 고치면 다른 곳이 깨진다.
- [ ] 변경 후 **검증을 실제로 수행**한다.
  - Python: `python -m py_compile <file>` 문법 확인 + 가능하면 생성 로직 단위 검증(실행 가능한 최소 테스트).
  - JSON: `python -c "import json; json.load(open(...))"` 유효성 확인.
  - 단순히 "컴파일 통과"가 아니라 **결과물(최종 full_script, title 등)이 의도대로 나오는지** 확인한다.

## 2. 푸시/커밋 전 필수 체크리스트

- [ ] `git status`로 변경 대상 파일 확인 (의도하지 않은 파일 포함 여부).
- [ ] `git diff`로 변경 내용 검토 (의도한 수정만 있는지).
- [ ] **프로젝트 문서 업데이트**: 커밋/푸시 전 이번 작업 내역에 맞춰 `WORK_LOG.md`, `ROADMAP.md` 등 프로젝트 관련 문서를 최신 상태로 갱신한다.
- [ ] **원격 상태 먼저 확인**: CI(GitHub Actions)가 `history/generated_topics.json` 등을 자동 커밋하므로
  **항상 `git fetch` 후 원격에 새 커밋이 있는지 확인**한다. 있다면 `git pull --rebase origin main`으로 병합 후 푸시.
  - 이 프로젝트에서 푸시 거부(`rejected`)가 반복 발생한 원인이 CI 자동 커밋이므로 미리 확인할 것.
- [ ] 커밋 메시지는 프로젝트 기존 스타일을 따른다 (예: `fix: ...`, `feat: ...`, `📝 [KO] Update history ...`).

## 3. 이 프로젝트에서 과거에 반복된 실수 (재발 방지)

이 프로젝트에서 실제로 반복된 문제들. 같은 실수를 다시 하지 않는다.

1. **한 곳을 고치면 다른 곳이 깨지는 연결 구조**
   - 예: 댓글 CTA 강제 append를 제거했더니 CTA가 아예 사라짐 → CTA 보장 로직 별도 추가 필요.
   - → 변경 전에 `full_script`/`cta`/`comment_cta` 의 상호작용을 먼저 파악한다.
2. **문자 정규화 시 실제 줄바꿈 vs 리터럴 `\n`(백슬래시+n) 구분 실패**
   - `' '.join(str(x).split())`은 실제 공백만 처리한다. 리터럴 `\n` 문자열은 `replace('\\n', ' ')`로 별도 처리해야 한다.
3. **루프 대본/접속사 마무리**
   - 모델이 "왜냐하면" 같은 접속사로 대본을 끝내면 영상이 끊긴 듯 들린다.
   - 프롬프트는 완결형 마침표 마무리를 명시하고, 코드에서도 매달린 접속사로 끝나면 거부한다.
4. **TTS 설정값 불일치**
   - `config.yml`의 `silence_between_sentences_ms`가 코드의 `silence_ms`로 읽힌다. 값 변경 시 반드시 매핑을 확인한다.
5. **CI 자동 커밋으로 인한 푸시 충돌**
   - `history/`는 CI가 자동 커밋하므로 푸시 전 원격 상태를 반드시 확인한다 (위 2번 항목).

## 4. 진행 원칙

- 사용자가 "꼼꼼하게 하자"고 한 만큼, **빠른 수정보다 정확한 수정**을 우선한다.
- 변경 전 계획을 간단히 말하고 승인받는다. 모호하면 질문한다.
- 결과물이 실제로 의도대로 나오는지 확인하는 것을 게을리하지 않는다.
